session:
  idle_timeout_seconds: 900        # 15 min
  window_seconds: 60               # rate window for behavior

thresholds:
  scanner_unique_paths_per_min: 30
  scanner_404_ratio: 0.6
  bruteforce_login_attempts_per_5min: 15
  bruteforce_fail_ratio: 0.8

output:
  events_jsonl: output/events.jsonl
  detections_json: output/detections.json
